import { StyleSheet } from 'react-native';

const settingsContainerStyle = StyleSheet.create({
  container: {
    flex:1, 
    paddingTop:20,
    paddingHorizontal: 10
  }
});

export default settingsContainerStyle;